// Generated by NailHammer. DO NOT EDIT.
//
// Edit the .nh grammar and re-run `nh build --rust`. Anything changed here is
// lost on the next build.

#![allow(dead_code, unused_imports)]

use nh_runtime::{FileId, Ident, Node, View};
use nh_runtime::pest::Pair;

use crate::Rule;

/// One matched `document`.
///
/// From this alternative of `rule document`:
///
/// ```text
/// SOI entries:entry* EOI -> doc
/// ```
#[derive(Clone, Debug)]
pub struct DocumentView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for DocumentView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        DocumentView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> DocumentView<'i> {

    /// `entries` — the `entry` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `entries: Vec<Self::Out>`:
    /// the value of the `entry` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn entries(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("entries")
    }
}

/// One matched `entry`.
///
/// From this alternative of `rule entry`:
///
/// ```text
/// key:IDENT "=" value:value ";" -> entry
/// ```
#[derive(Clone, Debug)]
pub struct EntryView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for EntryView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        EntryView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> EntryView<'i> {

    /// `key` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `key: &str`:
    /// the text of the `IDENT` token
    pub fn key(&self) -> Node<'i, Rule> {
        self.node.tagged("key").expect(
            "the grammar guarantees `key` is present; regenerate if it changed",
        )
    }

    /// `value` — the `value` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `value` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `value_string`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// raw:STRING -> string
/// ```
#[derive(Clone, Debug)]
pub struct ValueStringView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueStringView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueStringView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueStringView<'i> {

    /// `raw` — the `STRING` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `raw: &str`:
    /// the text of the `STRING` token
    pub fn raw(&self) -> Node<'i, Rule> {
        self.node.tagged("raw").expect(
            "the grammar guarantees `raw` is present; regenerate if it changed",
        )
    }
}

/// One matched `value_number`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// digits:NUMBER -> number
/// ```
#[derive(Clone, Debug)]
pub struct ValueNumberView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueNumberView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueNumberView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueNumberView<'i> {

    /// `digits` — the `NUMBER` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `digits: &str`:
    /// the text of the `NUMBER` token
    pub fn digits(&self) -> Node<'i, Rule> {
        self.node.tagged("digits").expect(
            "the grammar guarantees `digits` is present; regenerate if it changed",
        )
    }
}

/// One matched `value_yes`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// "true" -> yes
/// ```
#[derive(Clone, Debug)]
pub struct ValueYesView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueYesView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueYesView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueYesView<'i> {
}

/// One matched `value_no`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// "false" -> no
/// ```
#[derive(Clone, Debug)]
pub struct ValueNoView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueNoView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueNoView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueNoView<'i> {
}

/// One matched `value_null`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// "null" -> null
/// ```
#[derive(Clone, Debug)]
pub struct ValueNullView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueNullView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueNullView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueNullView<'i> {
}

/// One matched `value_list`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// "[" items:value* "]" -> list
/// ```
#[derive(Clone, Debug)]
pub struct ValueListView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueListView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueListView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueListView<'i> {

    /// `items` — the `value` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `items: Vec<Self::Out>`:
    /// the value of the `value` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn items(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("items")
    }
}

/// One matched `value_table`.
///
/// From this alternative of `rule value`:
///
/// ```text
/// "{" fields:entry* "}" -> table
/// ```
#[derive(Clone, Debug)]
pub struct ValueTableView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ValueTableView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ValueTableView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ValueTableView<'i> {

    /// `fields` — the `entry` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `fields: Vec<Self::Out>`:
    /// the value of the `entry` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn fields(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("fields")
    }
}

