// Generated by NailHammer. DO NOT EDIT.
//
// Edit the .nh grammar and re-run `nh build --rust`. Anything changed here is
// lost on the next build.

#![allow(dead_code, unused_imports)]

use nh_runtime::{FileId, Ident, Node, View};
use nh_runtime::pest::Pair;

use crate::Rule;

/// One matched `program`.
///
/// From this alternative of `rule program`:
///
/// ```text
/// SOI body:stmt+ EOI -> program
/// ```
#[derive(Clone, Debug)]
pub struct ProgramView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ProgramView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ProgramView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ProgramView<'i> {

    /// `body` — the `stmt` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: Vec<Self::Out>`:
    /// the value of the `stmt` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn body(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("body")
    }
}

/// One matched `stmt_print`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "print" value:expr ";" -> print
/// ```
#[derive(Clone, Debug)]
pub struct StmtPrintView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtPrintView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtPrintView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtPrintView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_iff`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "if" "(" cond:expr ")" lazy body:block -> iff
/// ```
#[derive(Clone, Debug)]
pub struct StmtIffView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtIffView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtIffView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtIffView<'i> {

    /// `cond` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `cond: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn cond(&self) -> Node<'i, Rule> {
        self.node.tagged("cond").expect(
            "the grammar guarantees `cond` is present; regenerate if it changed",
        )
    }

    /// `body` — the `block` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: &Shared<Block>`:
    /// the `block` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn body(&self) -> Node<'i, Rule> {
        self.node.tagged("body").expect(
            "the grammar guarantees `body` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_whilst`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "while" "(" lazy cond:expr ")" lazy body:block -> whilst
/// ```
#[derive(Clone, Debug)]
pub struct StmtWhilstView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtWhilstView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtWhilstView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtWhilstView<'i> {

    /// `cond` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `cond: &Shared<Expr>`:
    /// the `expr` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn cond(&self) -> Node<'i, Rule> {
        self.node.tagged("cond").expect(
            "the grammar guarantees `cond` is present; regenerate if it changed",
        )
    }

    /// `body` — the `block` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: &Shared<Block>`:
    /// the `block` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn body(&self) -> Node<'i, Rule> {
        self.node.tagged("body").expect(
            "the grammar guarantees `body` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_define`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "fn" name:IDENT "(" (params:IDENT ","?)* ")" lazy body:block -> define
/// ```
#[derive(Clone, Debug)]
pub struct StmtDefineView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtDefineView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtDefineView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtDefineView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }

    /// `params` — the `IDENT` token.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `params: &[String]`:
    /// the text of the `IDENT` token (repeated in the grammar)
    pub fn params(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("params")
    }

    /// `body` — the `block` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: &Shared<Block>`:
    /// the `block` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn body(&self) -> Node<'i, Rule> {
        self.node.tagged("body").expect(
            "the grammar guarantees `body` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_ret`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "return" value:expr ";" -> ret
/// ```
#[derive(Clone, Debug)]
pub struct StmtRetView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtRetView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtRetView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtRetView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_eval`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// value:expr ";" -> eval
/// ```
#[derive(Clone, Debug)]
pub struct StmtEvalView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtEvalView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtEvalView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtEvalView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `block`.
///
/// From this alternative of `rule block`:
///
/// ```text
/// "{" body:stmt+ "}" -> block
/// ```
#[derive(Clone, Debug)]
pub struct BlockView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for BlockView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        BlockView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> BlockView<'i> {

    /// `body` — the `stmt` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: Vec<Self::Out>`:
    /// the value of the `stmt` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn body(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("body")
    }
}

/// One matched `primary_num`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// value:NUMBER -> num
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryNumView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryNumView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryNumView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryNumView<'i> {

    /// `value` — the `NUMBER` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: &str`:
    /// the text of the `NUMBER` token
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `primary_str`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// text:STRING -> str
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryStrView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryStrView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryStrView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryStrView<'i> {

    /// `text` — the `STRING` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `text: &str`:
    /// the text of the `STRING` token
    pub fn text(&self) -> Node<'i, Rule> {
        self.node.tagged("text").expect(
            "the grammar guarantees `text` is present; regenerate if it changed",
        )
    }
}

/// One matched `primary_elem`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// name:IDENT "[" index:expr "]" -> elem place
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryElemView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryElemView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryElemView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryElemView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }

    /// `index` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `index: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn index(&self) -> Node<'i, Rule> {
        self.node.tagged("index").expect(
            "the grammar guarantees `index` is present; regenerate if it changed",
        )
    }
}

/// One matched `primary_call`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// name:IDENT "(" args:exprs? ")" -> call
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryCallView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryCallView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryCallView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryCallView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }

    /// `args` — the `exprs` rule.
    /// Optional in the grammar (`?`), so this may be `None`.
    ///
    /// Dispatch turns this into the handler parameter
    /// `args: Option<Self::Out>`:
    /// the value of the `exprs` rule, already evaluated (optional in the
    /// grammar)
    pub fn args(&self) -> Option<Node<'i, Rule>> {
        self.node.tagged("args")
    }
}

/// One matched `primary_var`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// name:IDENT -> var place
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryVarView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryVarView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryVarView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryVarView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }
}

/// One matched `primary_list`.
///
/// From this alternative of `rule primary`:
///
/// ```text
/// "[" first:expr rest:more_elem* "]" -> list
/// ```
#[derive(Clone, Debug)]
pub struct PrimaryListView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for PrimaryListView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        PrimaryListView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> PrimaryListView<'i> {

    /// `first` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `first: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn first(&self) -> Node<'i, Rule> {
        self.node.tagged("first").expect(
            "the grammar guarantees `first` is present; regenerate if it changed",
        )
    }

    /// `rest` — the `more_elem` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `rest: Vec<Self::Out>`:
    /// the value of the `more_elem` rule, already evaluated (repeated in
    /// the grammar; items that failed and were already reported are
    /// omitted)
    pub fn rest(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("rest")
    }
}

/// One matched `more_elem`.
///
/// From this alternative of `rule more_elem`:
///
/// ```text
/// "," value:expr -> next
/// ```
#[derive(Clone, Debug)]
pub struct MoreElemView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for MoreElemView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        MoreElemView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> MoreElemView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `exprs`.
///
/// From this alternative of `rule exprs`:
///
/// ```text
/// first:expr rest:more_elem* -> some
/// ```
#[derive(Clone, Debug)]
pub struct ExprsView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ExprsView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ExprsView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ExprsView<'i> {

    /// `first` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `first: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn first(&self) -> Node<'i, Rule> {
        self.node.tagged("first").expect(
            "the grammar guarantees `first` is present; regenerate if it changed",
        )
    }

    /// `rest` — the `more_elem` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `rest: Vec<Self::Out>`:
    /// the value of the `more_elem` rule, already evaluated (repeated in
    /// the grammar; items that failed and were already reported are
    /// omitted)
    pub fn rest(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("rest")
    }
}

/// View over `expr`, the rule supplied by the operator table.
///
/// Exposes the flat operand/operator stream. Folding it by precedence is
/// the M3 driver's job; until then a language that needs expressions can
/// override `Handlers::expr` and walk this itself.
#[derive(Clone, Debug)]
pub struct ExprView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ExprView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ExprView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

impl<'i> ExprView<'i> {
    /// Every direct child, operands and operators interleaved.
    pub fn parts(&self) -> Vec<Node<'i, Rule>> {
        self.node.children().collect()
    }

    /// The raw pairs the operator driver folds.
    pub fn pairs(&self) -> Vec<Pair<'i, Rule>> {
        self.node.pair().clone().into_inner().collect()
    }
}

