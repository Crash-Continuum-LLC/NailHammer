// Generated by NailHammer. DO NOT EDIT.
//
// Edit the .nh grammar and re-run `nh build --rust`. Anything changed here is
// lost on the next build.

#![allow(dead_code, unused_imports)]

use nh_runtime::{FileId, Ident, Node, View};
use nh_runtime::pest::Pair;

use crate::Rule;

/// One matched `program`.
///
/// From this alternative of `rule program`:
///
/// ```text
/// SOI body:stmt* EOI -> program
/// ```
#[derive(Clone, Debug)]
pub struct ProgramView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ProgramView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ProgramView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> ProgramView<'i> {

    /// `body` — the `stmt` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: Vec<Self::Out>`:
    /// the value of the `stmt` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn body(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("body")
    }
}

/// One matched `stmt_declare`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "let" name:IDENT "=" value:expr ";" -> declare
/// ```
#[derive(Clone, Debug)]
pub struct StmtDeclareView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtDeclareView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtDeclareView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtDeclareView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_show`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "show" value:expr ";" -> show
/// ```
#[derive(Clone, Debug)]
pub struct StmtShowView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtShowView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtShowView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtShowView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_branch`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "if" cond:expr lazy then:block lazy otherwise:else_tail? -> branch
/// ```
#[derive(Clone, Debug)]
pub struct StmtBranchView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtBranchView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtBranchView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtBranchView<'i> {

    /// `cond` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `cond: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn cond(&self) -> Node<'i, Rule> {
        self.node.tagged("cond").expect(
            "the grammar guarantees `cond` is present; regenerate if it changed",
        )
    }

    /// `then` — the `block` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `then: &Shared<Block>`:
    /// the `block` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn then(&self) -> Node<'i, Rule> {
        self.node.tagged("then").expect(
            "the grammar guarantees `then` is present; regenerate if it changed",
        )
    }

    /// `otherwise` — the `else_tail` rule.
    /// Optional in the grammar (`?`), so this may be `None`.
    ///
    /// Dispatch turns this into the handler parameter
    /// `otherwise: Option<&Shared<ElseTail>>`:
    /// the `else_tail` rule, **unevaluated** — `.eval(host, cx)?` runs
    /// it (optional in the grammar)
    pub fn otherwise(&self) -> Option<Node<'i, Rule>> {
        self.node.tagged("otherwise")
    }
}

/// One matched `stmt_loop`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "while" lazy cond:expr lazy body:block -> loop
/// ```
#[derive(Clone, Debug)]
pub struct StmtLoopView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtLoopView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtLoopView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtLoopView<'i> {

    /// `cond` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `cond: &Shared<Expr>`:
    /// the `expr` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn cond(&self) -> Node<'i, Rule> {
        self.node.tagged("cond").expect(
            "the grammar guarantees `cond` is present; regenerate if it changed",
        )
    }

    /// `body` — the `block` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: &Shared<Block>`:
    /// the `block` rule, **unevaluated** — `.eval(host, cx)?` runs it
    pub fn body(&self) -> Node<'i, Rule> {
        self.node.tagged("body").expect(
            "the grammar guarantees `body` is present; regenerate if it changed",
        )
    }
}

/// One matched `stmt_frame`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// "begin" "frame" lazy body:stmt* "end" "frame" -> frame
/// ```
#[derive(Clone, Debug)]
pub struct StmtFrameView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtFrameView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtFrameView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtFrameView<'i> {

    /// `body` — the `stmt` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: &[Shared<Stmt>]`:
    /// the `stmt` rule, **unevaluated** — `.eval(host, cx)?` runs it
    /// (repeated in the grammar)
    pub fn body(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("body")
    }
}

/// One matched `stmt_evaluate`.
///
/// From this alternative of `rule stmt`:
///
/// ```text
/// value:expr ";" -> evaluate
/// ```
#[derive(Clone, Debug)]
pub struct StmtEvaluateView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for StmtEvaluateView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        StmtEvaluateView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> StmtEvaluateView<'i> {

    /// `value` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `block`.
///
/// From this alternative of `rule block`:
///
/// ```text
/// "{" body:stmt* "}" -> block
/// ```
#[derive(Clone, Debug)]
pub struct BlockView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for BlockView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        BlockView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> BlockView<'i> {

    /// `body` — the `stmt` rule.
    /// Repeated in the grammar (`*` or `+`), so this may be empty.
    ///
    /// Dispatch turns this into the handler parameter
    /// `body: Vec<Self::Out>`:
    /// the value of the `stmt` rule, already evaluated (repeated in the
    /// grammar; items that failed and were already reported are omitted)
    pub fn body(&self) -> Vec<Node<'i, Rule>> {
        self.node.tagged_all("body")
    }
}

/// One matched `atom_number`.
///
/// From this alternative of `rule atom`:
///
/// ```text
/// value:NUMBER -> number
/// ```
#[derive(Clone, Debug)]
pub struct AtomNumberView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for AtomNumberView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        AtomNumberView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> AtomNumberView<'i> {

    /// `value` — the `NUMBER` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `value: &str`:
    /// the text of the `NUMBER` token
    pub fn value(&self) -> Node<'i, Rule> {
        self.node.tagged("value").expect(
            "the grammar guarantees `value` is present; regenerate if it changed",
        )
    }
}

/// One matched `atom_text`.
///
/// From this alternative of `rule atom`:
///
/// ```text
/// text:TEXT -> text
/// ```
#[derive(Clone, Debug)]
pub struct AtomTextView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for AtomTextView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        AtomTextView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> AtomTextView<'i> {

    /// `text` — the `TEXT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `text: &str`:
    /// the text of the `TEXT` token
    pub fn text(&self) -> Node<'i, Rule> {
        self.node.tagged("text").expect(
            "the grammar guarantees `text` is present; regenerate if it changed",
        )
    }
}

/// One matched `atom_name`.
///
/// From this alternative of `rule atom`:
///
/// ```text
/// name:IDENT -> name place
/// ```
#[derive(Clone, Debug)]
pub struct AtomNameView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for AtomNameView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        AtomNameView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> AtomNameView<'i> {

    /// `name` — the `IDENT` token.
    ///
    /// Dispatch turns this into the handler parameter
    /// `name: &str`:
    /// the text of the `IDENT` token
    pub fn name(&self) -> Node<'i, Rule> {
        self.node.tagged("name").expect(
            "the grammar guarantees `name` is present; regenerate if it changed",
        )
    }
}

/// One matched `atom_point`.
///
/// From this alternative of `rule atom`:
///
/// ```text
/// "(" x:expr "," y:expr ")" -> point
/// ```
#[derive(Clone, Debug)]
pub struct AtomPointView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for AtomPointView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        AtomPointView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

// Accessors are inherent, so a binding named `text` or `span` shadows
// the `View` method of that name rather than colliding with it.
impl<'i> AtomPointView<'i> {

    /// `x` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `x: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn x(&self) -> Node<'i, Rule> {
        self.node.tagged("x").expect(
            "the grammar guarantees `x` is present; regenerate if it changed",
        )
    }

    /// `y` — the `expr` rule.
    ///
    /// Dispatch turns this into the handler parameter
    /// `y: Self::Out`:
    /// the value of the `expr` rule, already evaluated
    pub fn y(&self) -> Node<'i, Rule> {
        self.node.tagged("y").expect(
            "the grammar guarantees `y` is present; regenerate if it changed",
        )
    }
}

/// View over `expr`, the rule supplied by the operator table.
///
/// Exposes the flat operand/operator stream. Folding it by precedence is
/// the M3 driver's job; until then a language that needs expressions can
/// override `Handlers::expr` and walk this itself.
#[derive(Clone, Debug)]
pub struct ExprView<'i> {
    node: Node<'i, Rule>,
}

impl<'i> View<'i, Rule> for ExprView<'i> {
    fn from_pair(pair: Pair<'i, Rule>, file: FileId) -> Self {
        ExprView { node: Node::new(pair, file) }
    }

    fn node(&self) -> &Node<'i, Rule> {
        &self.node
    }
}

impl<'i> ExprView<'i> {
    /// Every direct child, operands and operators interleaved.
    pub fn parts(&self) -> Vec<Node<'i, Rule>> {
        self.node.children().collect()
    }

    /// The raw pairs the operator driver folds.
    pub fn pairs(&self) -> Vec<Pair<'i, Rule>> {
        self.node.pair().clone().into_inner().collect()
    }
}

